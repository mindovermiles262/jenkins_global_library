@Library('my-jgl') _

pipeline {
  agent {
    kubernetes {
      containerTemplate {
        name 'my-container'
        image 'alpine:latest'
        label 'alpine-container'
        command 'cat'
        ttyEnabled true
      }
    }
  }
  stages {
    stage("Smoke Test") {
      steps {
        sh "hostname"
        sh "ip a"
      }
    }
  }
}

