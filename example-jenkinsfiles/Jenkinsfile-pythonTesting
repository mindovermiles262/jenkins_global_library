@Library('my-jgl') _
pipeline {
  agent {
    kubernetes {
      containerTemplate {
        name 'my-groovy'
        image 'groovy'
      }
    }
  }
  stages {
    stage('JGL Shared Methods') {
      steps {
        script {
          dataeng.unitTest()
        }
      }
    }
  }
}
