@Library('my-jgl') _
pipeline {
  agent {
    kubernetes {
      containerTemplate {
        name 'my-groovy'
        image 'groovy'
      }
      containerTemplate {
        name 'my-unit-testing'
        image 'python:3.7-alpine'
      }
    }
  }
  stages {
    stage('JGL Shared Methods') {
      container('my-unit-testing')
      steps {
        script {
          dataeng.unitTest()
        }
      }
    }
  }
}
