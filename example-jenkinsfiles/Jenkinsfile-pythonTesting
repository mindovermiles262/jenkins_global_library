@Library('my-jgl') _
pipeline {
  agent {
    kubernetes {
      containerTemplate {
        name 'my-groovy'
        image 'groovy'
      }
      containerTemplate {
        name 'python-testing'
        image 'python'
      }
    }
  }
  stages {
    stage('JGL Shared Methods') {
      steps {
        script {
          dataeng.unitTest()
        }
      }
    }
  }
}
